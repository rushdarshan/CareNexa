# EcoMedAi Environment Variables
# Copy this file to .env.local and fill in values
# ──────────────────────────────────────────────

# Gemini API key — SERVER SIDE ONLY (no NEXT_PUBLIC_ prefix!)
# This key is NEVER exposed to the browser.
GEMINI_API_KEY=your_gemini_api_key_here

# App URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: Firebase (for persistent community pins)
# NEXT_PUBLIC_FIREBASE_API_KEY=
# NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
# NEXT_PUBLIC_FIREBASE_DATABASE_URL=
# NEXT_PUBLIC_FIREBASE_PROJECT_ID=

# Optional: Google Maps (for real map integration in CommunityHealthMap)
# NEXT_PUBLIC_GOOGLE_MAPS_KEY=

# ⚠️ DO NOT add NEXT_PUBLIC_GEMINI_API_KEY — this exposes your key to the browser!
# All AI calls should go through /api/chat, /api/health-insights, /api/ocr etc.
